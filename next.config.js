// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  images: {
    domains: ['lh3.googleusercontent.com'], // Agrega el dominio de las imágenes que usas
  },
  // otras configuraciones
};

module.exports = nextConfig;
